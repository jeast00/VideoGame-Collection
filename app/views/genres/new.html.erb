<h1>Add a Genre</h1>

<%= form_for(@genre) do |f| %>
    <%= f.label :genre_type %>
    <%= f.text_field :genre_type %>
    <%= f.submit "Add Genre" %>
<% end %>

<%= link_to "go back", user_path(current_user.id) %>